========
REST API
========

.. include:: ./apispec.json

..
   /api.html is rendered by sphinxcontrib-redoc.
   This rst file doesn't really render.
   The `include` line is just to trigger sphinx-autobuild when apispec.json changes
   Run build_apispec.py to generate the apispec.json file